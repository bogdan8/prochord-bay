<p><%= t('views.mailer.welcome') %> <%= @resource.email %>!</p>

<p><%= t('views.mailer.reset_password_body') %></p>

<p><%= link_to t('views.mailer.reset_password_link'), edit_password_url(@resource, reset_password_token: @token) %></p>

<p><%= t('views.mailer.reset_password_warning') %></p>
<p><%= t('views.mailer.reset_password_warning_two') %></p>
