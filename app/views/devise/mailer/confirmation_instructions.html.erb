<p><%= t('views.mailer.welcome') %> <%= @email %>!</p>

<p><%= t('views.mailer.send_confirm') %></p>

<p><%= link_to t('views.mailer.link_confirm'), confirmation_url(@resource, confirmation_token: @token) %></p>
